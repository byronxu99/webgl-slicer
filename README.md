# webgl-slicer

## Production build
* `npm install`
* `npm run build`

## Development
* `npm install`
* `npm start` (runs `webpack-serve` to auto-update upon changes)
* open browser to `localhost:8080`

